let arr = []; // starting index -> 0

console.log(arr);

arr.push('jeel'); // add element at the last index
arr.push('parekh'); 
arr.unshift('raviraj'); // add element at the starting index
console.log(arr);

arr.pop(); // remove element from the last
let result = arr.shift(); // remove the element from starting index
console.log(result);